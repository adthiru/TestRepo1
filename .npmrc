# Pin the public npm registry explicitly. registry.npmjs.org is covered by the
# AutoFab MDE egress allowlist (.npmjs.org), so installs work in a sandbox with
# no VPC attached. Do not point this at an internal or proxy registry.
registry=https://registry.npmjs.org/
audit=false
fund=false
