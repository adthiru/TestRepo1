{
  "name": "testrepo1",
  "version": "1.0.0",
  "private": true,
  "description": "Small Express service used as a public-artifact dry run for the Release Readiness Review agent on AutoFab",
  "license": "Apache-2.0",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --runInBand",
    "test:coverage": "jest --runInBand --coverage",
    "lint": "eslint .",
    "build": "node scripts/build.js"
  },
  "dependencies": {
    "dayjs": "^1.11.13",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
